name: Time Simulator Plugin
version: 1.0.0
id: time-simulation-plugin
extensions:
  - id: time-simulation
    type: widget
    name: Time Simulation
    widgetLayout:
      defaultLocation:
        zone: outer
        section: right
        area: top
    schema:
     


